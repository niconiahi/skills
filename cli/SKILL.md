---
name: cli
description: "Keep to a project's defined CLI commands (just/make/npm scripts). Use when a defined command doesn't exactly fit the task — never reach for an ad-hoc escape-hatch command; rework the defined command so it works as intended instead."
---

# CLI

- You will not try escape-hatch command when one that is defined doesn't exactly fit the problem. In such case, you'll rework the already defined command so it will work as it was intended
